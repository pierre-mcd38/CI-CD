<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Convertisseur de Devises</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #000000;
            padding: 50px;
        }
        .container {
            max-width: 500px;
            margin: auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        input, select, button {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
        }
        button {
            background-color: #0076f5;
            color: white;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s;
        }
        button:hover {
            background-color: #0056b3;
        }
        .result {
            margin-top: 20px;
            font-size: 1.2em;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Convertisseur de Devises</h2>
        <form action="/convert" method="POST">
            <label for="amount">Montant à Convertir :</label>
            <input type="number" id="amount" name="amount" required step="0.01" min="0">

            <label for="from_currency">Devise d'origine :</label>
            <select id="from_currency" name="from_currency" required>
                <option value="USD">Dollar Américain (USD)</option>
                <option value="EUR">Euro (EUR)</option>
                <option value="GBP">Livre Sterling (GBP)</option>
                <option value="JPY">Yen Japonais (JPY)</option>
                <!-- Ajoutez plus de devises ici si nécessaire -->
            </select>

            <label for="to_currency">Devise de destination :</label>
            <select id="to_currency" name="to_currency" required>
                <option value="EUR">Euro (EUR)</option>
                <option value="USD">Dollar Américain (USD)</option>
                <option value="GBP">Livre Sterling (GBP)</option>
                <option value="JPY">Yen Japonais (JPY)</option>
                <!-- Ajoutez plus de devises ici si nécessaire -->
            </select>

            <button type="submit">Convertir</button>
        </form>

        <% if (result) { %>
        <div class="result">
            <h3>
                <%= amount %> <%= from_currency %> = <strong><%= result %> <%= to_currency %></strong>
            </h3>
        </div>
        <% } %>
    </div>
</body>
</html>
